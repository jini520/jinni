# 운영환경 설정
# application-prod.properties

# 데이터베이스 연결 설정
spring.datasource.url=ENC(XZUkpEec2/fKws6HZnqZ4z9PAqJppZNLKwD95PUkMbX2Xc1EzY8RUP5Mwg8ivFschUbtQh5xMTs=)
spring.datasource.username=ENC(Fd38nA8wFfyH4/l2LrdkqQ==)
spring.datasource.password=ENC(n/IEbZt6HIjaGZfS7+T0XlX9uK6A2ZZj)
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA 설정
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=0

# 서버 설정
server.port=8080

# 로깅 설정
logging.level.root=WARN

# 파일 저장 경로 설정 (이미지와 문서 분리)
file.upload-dir.images=/app/uploads/images
file.upload-dir.documents=/app/uploads/documents
file.upload-dir.resumes=/app/uploads/resumes
file.upload-dir.portfolios=/app/uploads/portfolios

# 파일 업로드 제한 설정
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
spring.servlet.multipart.resolve-lazily=true

# Velog RSS
velog.rss-url=https://v2.velog.io/rss
velog.username=ENC(51AyReEYUi5GxQz1rSzE4Wjt4gG9DAmf)

# 주의: 운영 환경에서는 반드시 JASYPT_ENCRYPTOR_PASSWORD 환경 변수를 설정해야 합니다
# spring.datasource.username은 암호화되어 있습니다
# spring.datasource.password은 암호화되어 있습니다
